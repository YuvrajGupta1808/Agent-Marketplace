[build]
builder = "NIXPACKS"
buildCommand = "pip install -e . && cd ui && npm ci && npm run build"

[deploy]
startCommand = "python scripts/start.py"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
